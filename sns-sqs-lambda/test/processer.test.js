describe('processor', () => {
  describe('process', () => {
    describe('when the method runs correctly', () => {
      it('is a placeholder', () => {
        expect(1).toBe(1)
      })
    })
  })
})
